/**
 * @querais/contracts — public surface for downstream TS packages.
 *
 *  - ABIs (`as const`) for full viem typing: quaisTokenAbi, nodeRegistryAbi, jobEscrowAbi,
 *    creditAccountAbi
 *  - loadAddresses(network): the deployed addresses + named dev accounts
 */
export * from './abis.js';
export * from './addresses.js';

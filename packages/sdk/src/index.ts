/**
 * @querais/sdk — a tiny OpenAI-shaped TypeScript client + `querais` CLI.
 *
 * Lets a developer point existing OpenAI code at the QueraIS gateway. Built in M4.
 * For M0 this is an empty skeleton.
 */

export const SDK_VERSION = '0.1.0';
